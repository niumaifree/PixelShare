kind = "web"
previewPath = "/"
title = "PixelShare"
version = "1.0.0"
id = "artifacts/pixelshare"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 5000

[services.development]
run = "pnpm --filter @workspace/pixelshare run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/pixelshare", "run", "build" ]
publicDir = "artifacts/pixelshare/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "5000"
BASE_PATH = "/"
